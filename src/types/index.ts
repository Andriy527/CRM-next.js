export interface Iuser {
    breakInterval: number,
    createdAt: string,
    email: string,
    id: string,
    intervalsCount: number,
    name: string,
    updatedAt: string,
    workInterval: number
}